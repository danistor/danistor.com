/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true, // Recommended for catching potential problems
  // i18n configuration removed - not supported in App Router
}

module.exports = nextConfig
